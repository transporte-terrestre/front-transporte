#!/bin/bash

BASE="/Users/erixcel/chamba/transporte terrestre/front-transporte/src/modules/admin/content"

# Function to apply role guard to list HTML files
# Pattern: Wrap "Nuevo" button with @if (isAdmin()) { ... }
# Pattern: Wrap edit/delete action buttons with @if (isAdmin()) { ... }
# Pattern: Wrap Acciones th with @if (isAdmin()) { ... }

echo "Script created successfully"
